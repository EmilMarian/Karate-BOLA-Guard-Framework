{
  "method": "GET",
  "parameters": [
    {
      "description": "ID of order that needs to be fetched",
      "in": "path",
      "name": "orderId",
      "required": true,
      "schema": {
        "format": "int64",
        "type": "integer"
      }
    }
  ],
  "path": "/store/order/{orderId}",
  "requestBody": {},
  "responses": {
    "404": {
      "description": "Order not found"
    }
  }
}